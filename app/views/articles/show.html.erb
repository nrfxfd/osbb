<h2><%= @article.title %></h2>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Редагувати", edit_article_path(@article) %></li>
  <li><%= link_to "Видалити", article_path(@article),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>

<h3>Коментарі</h3>
<%= render @article.comments %>

<h5>Додати коментар:</h5>

<%= render 'comments/form' %>

